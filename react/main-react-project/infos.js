// nvm install node  
// create vite : vite is used to craete react app 
// What is React?

// React is a UI library for building interfaces.

// React by itself:

// does NOT run in the browser automatically

// needs a build tool (like Vite, Webpack, etc.)

// npm create vite@latest follow the steps the cd project 
// han run npm install, npm run dev 